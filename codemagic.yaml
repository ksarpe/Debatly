# Codemagic CI — build iOS bez Maca i publikacja do TestFlight.
#
# Wymagana jednorazowa konfiguracja w UI Codemagic (wykonana; kroki poniżej):
#
# 1. codemagic.io → zaloguj przez GitHub → Add application → wybierz to repo → Flutter App.
# 2. Team settings (ikona zębatki przy teamie) → Integrations → Developer Portal →
#    dodaj klucz App Store Connect API (rola App Manager) i nazwij go DOKŁADNIE:
#    "AppStoreConnect" (nazwa musi zgadzać się z `integrations.app_store_connect` poniżej).
# 3. W aplikacji → Environment variables → utwórz grupę "debatly_secrets" i dodaj
#    (każdą jako Secure):
#      SUPABASE_URL              — https://….supabase.co
#      SUPABASE_ANON_KEY         — anon key z Supabase
#      REVENUECAT_API_KEY_IOS    — klucz appl_… (RevenueCat → apka App Store → Public API Key)
#      GOOGLE_SERVER_CLIENT_ID   — ten sam web client ID co na Androidzie
#      SENTRY_DSN                — DSN z Sentry
#    Opcjonalnie (czytelne, deobfuskowane stack trace w Sentry — patrz SENTRY_SETUP.md §5):
#      SENTRY_AUTH_TOKEN         — org auth token 'sntrys_…' (Sentry → Settings → Auth Tokens);
#                                  token organizacyjny sam wybiera region EU (.de). To JEDYNY
#                                  potrzebny tu sekret — org (akn-software) i project (debatly)
#                                  są zaszyte w bloku `sentry:` w pubspec.yaml.
#    Bez SENTRY_AUTH_TOKEN krok wgrywania symboli jest po prostu pomijany (build i tak przechodzi).
#    ADMOB_REWARDED_ID_IOS z czasów freemium jest już nieużywany — można go z grupy usunąć.
# 4. Start new build → workflow "ios-testflight".
#
# Pierwszy krok builda ("Verify release configuration") twardo przerywa build,
# jeśli brakuje SUPABASE_URL / SUPABASE_ANON_KEY / REVENUECAT_API_KEY_IOS albo
# klucz RC ma zły kształt. Przy twardym paywallu taki artefakt to aplikacja,
# której nikt nie może kupić — lepiej wywalić się tu niż w sklepie.
#
# Numer builda (CFBundleVersion) bierze się z auto-inkrementującego licznika
# Codemagic ($BUILD_NUMBER) — każdy build dostaje unikalny, rosnący numer, więc
# nie trzeba go ręcznie podbijać w pubspec. Nazwa wersji
# (CFBundleShortVersionString) pochodzi z pubspec.yaml (pole `version:`, np. 1.0.1).

workflows:
  ios-testflight:
    name: iOS -> TestFlight
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: AppStoreConnect
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
      # Automatyczne podpisywanie: Codemagic sam tworzy/pobiera certyfikat dystrybucyjny
      # i provisioning profile dla bundle ID przez App Store Connect API — zero Xcode.
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.aknsoftware.debatly
      groups:
        - debatly_secrets
    scripts:
      - name: Flutter packages
        script: flutter pub get
      - name: Verify release configuration
        script: |
          # Runs FIRST (before the ~10 min signing + build) because these
          # secrets decide whether the artifact is usable at all.
          #
          # Debatly is a HARD PAYWALL: the entire app sits behind a purchase.
          # A release IPA without a RevenueCat key is therefore not "degraded",
          # it is DEAD — nobody can buy, so every new user is stuck on a wall
          # whose offer never loads. And the keyless mock-premium fallback in
          # SessionNotifier._load needs BOTH Supabase and RevenueCat absent, so
          # it does not rescue a build that has one and not the other.
          #
          # Deliberately no `set -e`: the checks below accumulate, so ONE build
          # reports every missing secret instead of one per attempt.
          fail=0

          if [ -z "$SUPABASE_URL" ]; then
            echo "❌ SUPABASE_URL is empty — no backend, no questions."
            fail=1
          fi
          if [ -z "$SUPABASE_ANON_KEY" ]; then
            echo "❌ SUPABASE_ANON_KEY is empty — no backend, no questions."
            fail=1
          fi

          # Mirrors PurchasesService.isUsableKey. The native RevenueCat SDK
          # ABORTS THE PROCESS on an invalid key ("app will close now to
          # protect the security"), so a placeholder ships worse than nothing.
          case "$REVENUECAT_API_KEY_IOS" in
            "")
              echo "❌ REVENUECAT_API_KEY_IOS is empty — nobody could ever buy PRO."
              fail=1 ;;
            *REPLACE*)
              echo "❌ REVENUECAT_API_KEY_IOS is still the env-file placeholder."
              fail=1 ;;
            test_*)
              echo "❌ REVENUECAT_API_KEY_IOS is a legacy 'test_' sample key."
              fail=1 ;;
            appl_*)
              ;;  # the only shape an iOS build can use
            *)
              echo "❌ REVENUECAT_API_KEY_IOS must start with 'appl_'."
              echo "   A goog_… (Android) key builds fine here and then fails on device."
              fail=1 ;;
          esac

          # Not fatal — the app still runs and still sells.
          if [ -z "$GOOGLE_SERVER_CLIENT_ID" ]; then
            echo "⚠️  GOOGLE_SERVER_CLIENT_ID empty — Google sign-in is disabled."
            echo "   Sign in with Apple still covers iOS, so PRO stays recoverable."
          fi
          if [ -z "$SENTRY_DSN" ]; then
            echo "⚠️  SENTRY_DSN empty — release crashes will go unreported."
          fi

          if [ "$fail" != 0 ]; then
            echo ""
            echo "Release configuration invalid — refusing to build a dead app."
            echo "Fix in Codemagic → app → Environment variables → 'debatly_secrets'."
            exit 1
          fi
          echo "✅ Release configuration OK."
      - name: Apply signing profiles to Xcode project
        script: xcode-project use-profiles
      - name: Validate privacy manifests (ITMS-91064 guard)
        script: |
          # ROOT CAUSE of the v1.0.7 build 7-11 ITMS-91064 rejections (nailed 2026-07-18
          # by inspecting build 11's IPA): App Store validation treats NSPrivacyTracking
          # and NSPrivacyTrackingDomains as a PAIR — tracking=true REQUIRES a non-empty
          # domains list. Proven on this app: true + empty array rejected (builds 7-10),
          # true + ABSENT domains rejected too (build 11). TN3181's table doesn't spell
          # out the second case and the email quotes yet another rule — do not trust
          # either; trust the IPA. Correct state for this app: NEITHER key present
          # (first-party code contacts no tracking domains; declaring Google's ad domains
          # would make iOS block them when ATT is denied). GMA's own manifest ships the
          # same keyless state. The source file is fixed; this step stays as a guard.
          #
          # It scans every *.xcprivacy reachable by the build (repo + SPM checkouts/ and
          # binary artifacts/ under DerivedData) and heals both invalid states:
          #   1. NSPrivacyTrackingDomains non-empty while NSPrivacyTracking != true
          #      -> force NSPrivacyTracking=true
          #   2. NSPrivacyTracking=true while NSPrivacyTrackingDomains is EMPTY or ABSENT
          #      -> remove BOTH keys (removing just the domains key leaves true+absent,
          #         which build 11 proved still gets rejected)
          # FULLY NON-FATAL (set +e / exit 0): can never break the build; the [privacy]
          # log lines name every manifest + its state for follow-up.
          set +e
          flutter build ios --config-only --no-codesign >/dev/null 2>&1
          xcodebuild -resolvePackageDependencies \
            -workspace ios/Runner.xcworkspace -scheme Runner >/dev/null 2>&1
          echo "=== [privacy] scanning all privacy manifests ==="
          find "$HOME/Library/Developer/Xcode/DerivedData" "$CM_BUILD_DIR" \
            -name '*.xcprivacy' 2>/dev/null | sort -u | while IFS= read -r f; do
            [ -z "$f" ] && continue
            # plutil raw on an array prints its element count; absent key errors out.
            domcount=$(/usr/bin/plutil -extract NSPrivacyTrackingDomains raw "$f" 2>/dev/null)
            [ -z "$domcount" ] && domcount=absent
            trk=$(/usr/bin/plutil -extract NSPrivacyTracking raw "$f" 2>/dev/null)
            [ -z "$trk" ] && trk=absent
            echo "[privacy] domains=$domcount tracking=$trk :: $f"
            if [ "$domcount" != absent ] && [ "$domcount" -gt 0 ] 2>/dev/null && [ "$trk" != "true" ]; then
              chmod u+w "$f" 2>/dev/null
              if /usr/bin/plutil -replace NSPrivacyTracking -bool true "$f" 2>/dev/null; then
                echo "[privacy]   >> FIX: set NSPrivacyTracking=true (domains present)"
              else
                echo "[privacy]   >> WARNING could not patch: $f"
              fi
            elif [ "$trk" = "true" ] && { [ "$domcount" = "0" ] || [ "$domcount" = absent ]; }; then
              chmod u+w "$f" 2>/dev/null
              /usr/bin/plutil -remove NSPrivacyTrackingDomains "$f" 2>/dev/null
              if /usr/bin/plutil -remove NSPrivacyTracking "$f" 2>/dev/null; then
                echo "[privacy]   >> FIX: removed tracking key pair (true without domains is rejected)"
              else
                echo "[privacy]   >> WARNING could not patch: $f"
              fi
            fi
          done
          echo "=== [privacy] done ==="
          exit 0
      - name: Build IPA
        script: |
          # --obfuscate + --split-debug-info produce the Dart symbol maps that
          # the Sentry upload step below needs to de-obfuscate release traces.
          flutter build ipa --release \
            --build-number=$BUILD_NUMBER \
            --export-options-plist=/Users/builder/export_options.plist \
            --obfuscate --split-debug-info=build/debug-info \
            --dart-define=SUPABASE_URL=$SUPABASE_URL \
            --dart-define=SUPABASE_ANON_KEY=$SUPABASE_ANON_KEY \
            --dart-define=REVENUECAT_API_KEY=$REVENUECAT_API_KEY_IOS \
            --dart-define=GOOGLE_SERVER_CLIENT_ID=$GOOGLE_SERVER_CLIENT_ID \
            --dart-define=SENTRY_DSN=$SENTRY_DSN \
            --dart-define=SENTRY_ENVIRONMENT=production
      - name: Upload debug symbols to Sentry
        script: |
          # Skipped automatically until you add SENTRY_AUTH_TOKEN to debatly_secrets,
          # so a missing token never fails the build. org/project + all other options
          # come from the top-level `sentry:` block in pubspec.yaml.
          if [ -z "$SENTRY_AUTH_TOKEN" ]; then
            echo "SENTRY_AUTH_TOKEN not set — skipping Sentry symbol upload."
            exit 0
          fi
          dart run sentry_dart_plugin
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        # Upload do TestFlight po udanym buildzie; do App Store wysyłasz ręcznie
        # z App Store Connect, kiedy wersja jest gotowa.
        submit_to_testflight: true
      email:
        recipients:
          - kasper@aknsoftware.com
        notify:
          success: true
          failure: true
